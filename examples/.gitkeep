A place for self-contained project example folders. This will prevent link and bit rot in the future, because maintaining a single repository with such projects is simpler than having many disjoint repositories under different GitHub organizations and user accounts.
