---
title: "How to retrieve wallet information, jettons, and NFTs with WalletKit on the iOS platform"
sidebarTitle: "Receiving information"
---

import { Aside } from '/snippets/aside.jsx';

Retrieve wallet information, Jettons, and NFTs using WalletKit.

<Aside type="tip">
  All methods require an existing wallet instance. [Create or retrieve](/ecosystem/ton-connect/walletkit/ios/wallets) a wallet before accessing data.
</Aside>

## Wallet information

### Balance

```swift
// Returns balance in nanoTON
let balance = try await wallet.balance()
```

### Address

```swift
let address = wallet.address
```

## Jettons

### Get all Jettons

```swift
let jettons = try await wallet.jettons(limit: TONLimitRequest(limit: 10, offset: 0))
```

### Get specific Jetton balance

```swift
let jettonAddress = "EQCxE..."
// Returns balance in nano with decimals count according to Jetton specification
let balance = try await wallet.jettonBalance(jettonAddress: jettonAddress)
```

### Get Jetton wallet address

```swift
let jettonAddress = "EQCxE..."
let walletAddress = try await wallet.jettonWalletAddress(jettonAddress: jettonAddress)
```

## NFTs

### Get all NFTs

```swift
let nfts = try await wallet.nfts(limit: TONLimitRequest(limit: 10, offset: 0))
```

### Get specific NFT

```swift
let nftAddress = "EQCA..."
let nft = wallet.nft(address: nftAddress)
```

## Next steps

<CardGroup cols={2}>
  <Card
    title="Transactions"
    href="./transactions"
  >
    Transfer TONs, Jettons and NFTs
  </Card>

  <Card
    title="Manage wallets"
    href="./wallets"
  >
    Create and manage TON wallets
  </Card>
</CardGroup>
